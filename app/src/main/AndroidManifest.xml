<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwiftServe"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".feature.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SwiftServe.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".feature.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".feature.dashboard.DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".feature.dashboard.SavedActivity"
            android:exported="false" />

        <activity
            android:name=".feature.dashboard.MyOrdersActivity"
            android:exported="false" />

        <activity
            android:name=".feature.profile.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".feature.profile.UpdateProfileActivity"
            android:exported="false" />

        <activity
            android:name=".feature.profile.ChangePasswordActivity"
            android:exported="false" />

        <activity
            android:name=".feature.checkout.CheckoutActivity"
            android:exported="false" />

        <activity
            android:name=".feature.payment.PaymentActivity"
            android:exported="false" />

        <activity
            android:name=".feature.payment.AfterPaymentActivity"
            android:exported="false" />

        <activity
            android:name=".feature.about.AboutUsActivity"
            android:exported="false" />

    </application>

</manifest>
